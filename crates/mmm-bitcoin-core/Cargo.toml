[package]
name = "mmm-bitcoin-core"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Bitcoin Core node access and the Core-backed parent classification policy."

[lints]
workspace = true

[features]
db-integration = []

[dependencies]
mmm-capture.workspace = true
mmm-rpc.workspace = true
anyhow.workspace = true
bitcoin.workspace = true
corepc-client.workspace = true
jsonrpc.workspace = true
tokio.workspace = true
tracing.workspace = true

[dev-dependencies]
# Only linked by the test-only scripted Core RPC fixture
# (parent_classifier::core_fixture): building and parsing the raw JSON-RPC
# envelopes it serves over a std::net::TcpListener. Both are already
# transitive dependencies via corepc-client/jsonrpc.
hex.workspace = true
serde_json.workspace = true
